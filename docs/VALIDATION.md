# Проверка 9 сентября 2026

| Проверка | Результат |
|---|---|
| pytest | 22 passed |
| Android assembleDebug | Успешно |
| Android testDebugUnitTest | 2 теста, 0 ошибок |
| Android lintDebug | Успешно, без ошибок; рекомендации по стилю и версиям зависимостей остаются |
| Python compileall | Успешно |
| JavaScript node --check | Успешно |
| Живой HTTP + реальный local STT | Русский WAV 25 секунд распознан, БД и Markdown созданы |
| Повтор с HF_HUB_OFFLINE=1 | Успешно, повторная загрузка модели из интернета не нужна |
| Сохранность аудио HTTP E2E | Скачанное аудио совпало с оригиналом |
| Запуск/остановка | Проверены штатными Windows-скриптами |

Управляемый тестовый конвейер с mock STT/LLM проходит до saved. Настоящий STT проходит до awaiting_ai, поскольку пользователь отложил API-ключ. Настоящий AI-конспект и задачи не проверены; их нельзя считать готовым пользовательским результатом до подключения API и проверки содержания.

Android unit-тесты не заменяют испытания микрофона и Bluetooth. Физический Android не был подключён. Список ручных сценариев — ANDROID.md. APK предназначен для следующего шага испытаний на устройстве пользователя.

Серверные проверки отдельно покрывают: конфликты UUID, повреждённое аудио, отсутствие авторизации, ограничение размера/текста, исчерпание повторов, неповторяемые ошибки, сохранение транскрипта при сбое AI, сохранение AI checkpoint при ошибке Markdown, повторный старт, правки без замены raw transcript и удаление файлов/FTS.

В testclient остаются два предупреждения об устаревающих внутренних API Starlette/AnyIO; тесты проходят. Версии установленных библиотек закреплены в requirements.lock.txt.
